"use client";
import Link from "next/link";
import {useState} from "react";
export default function Partner(){const [code,setCode]=useState('');return <section className="partner-page"><div className="partner-box"><img src="/images/logo.png" alt="Sun Sky Inn" width={175} height={175} loading="eager" decoding="async"/><span className="eyebrow">Exclusive invitation</span><h1>Welcome through one of our trusted travel partners</h1><p>Enter the private code you received. We will verify it directly and confirm the benefits attached to your invitation.</p><label className="sr-only" htmlFor="partner-code">Partner invitation code</label><input id="partner-code" aria-describedby="partner-code-privacy" value={code} onChange={e=>setCode(e.target.value.toUpperCase())} placeholder="Invitation code"/><a className={`btn btn-gold ${!code?'disabled':''}`} href={code?`https://wa.me/9609910136?text=${encodeURIComponent('Hello Sun Sky Inn, I was referred by a travel partner. My private code is: '+code)}`:'#'}>Verify my benefits</a><Link className="text-link" href="/">Return to website</Link><small id="partner-code-privacy">Your code is never displayed publicly and does not change the standard booking experience for other guests.</small></div></section>}
